<%= form_for(@quote) do |f| %>
  <% if @quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @quote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= text_area(:quote, :description, :cols => 40, :rows => 10) %>
  </div>
  <div class="field">
    <%= f.label :cost_per_hour %><br />
    <%= f.text_field :cost_per_hour %>
  </div>
  <div class="field">
    <%= f.label :hours_per_day %><br />
    <%= f.number_field :hours_per_day %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
